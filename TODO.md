## TODO
- Compatibility export (convert circles to polygons with the specified resulotion)
- Save the last center of the map before the app closes inside the project file so it can be restored
- Ability to add overlay image to the map (both in free style moving it by hand and adding using accurate coordinates) 
- Saving project as a single file that can contain the overlay images (Tarball for example)
- Ability to export as other common GIS formats like KML

## IDEAS
- Ability to add users custom map APIs?